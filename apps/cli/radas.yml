name: investrack
description: "App Investrack"
type: web-app
stacks:
  - nextjs
  - typescript
  - biome
  - tailwindcss
  - react-query
  - zod
  - react
contract:
  # No design contract exists in this repository; gen-styles falls back to
  # its explicit --source flag when this list is empty.
  api:
    - path: contracts/radas-api-v2.openapi.json
      type: open-api
