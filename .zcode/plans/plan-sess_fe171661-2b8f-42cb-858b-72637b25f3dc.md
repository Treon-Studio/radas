## Test Case contract batch

1. Add deterministic assertions to `services/test_cases.py`:
   - provider image outdated;
   - monthly budget exceeded;
   - instance count threshold;
   - missing environment/owner tags.
2. Validate test-case tags, parameters, and assertion IDs strictly instead of silently dropping invalid entries.
3. Add bounded external-tool execution helper for `tofu validate`, `tflint`, `checkov`, and `tfsec`:
   - allowlisted commands only;
   - timeout and output cap;
   - unavailable-tool result is explicit and non-fatal for mock mode.
4. Extend run result schema with stable `run_id`, tool metadata, timeout status, and mock-provider mode; export route returns stable JSON schema.
5. Add focused tests for all new assertions, validation, mock execution, timeout/export/run_id; retain existing scheduled/reliability tests.
6. Add only minimal console result/export visibility if existing test route supports it; run full backend, compileall, console typecheck/build, Playwright, and git diff check.